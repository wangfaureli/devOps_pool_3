matrix:
  include:
    - language: node_js
      node_js: '12.2.0'
      
      cache:
        directories:
          - node_modules

      script:
        - cd webinterfaces
        - npm install
        - npm run serve

      # deploy:
      #   skip_cleanup: true

      #   provider: script
      #   script: ./deploy.sh

      #   on:
      #     branch: main
      #     node: '5'